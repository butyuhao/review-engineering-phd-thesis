\documentclass{book}
\begin{document}
\chapter{桥梁结构动力响应模型}
\paragraph{边界条件。}本文建立梁桥动力模型。
\paragraph{参数识别}模型参数由现场测试确定。
图~\ref{fig:missing} 给出了有限元网格，相关模型见 \citep{missing2025}。
\begin{figure}[htbp]
  \caption{桥梁有限元模型。}
\end{figure}
\begin{table}[htbp]
  \caption{不同工况下的位移响应}
  \label{tab:response}
  \begin{tabular}{cc}工况 & 位移\\A & 1.2\end{tabular}
\end{table}
\begin{equation}\label{eq:duplicate}x=1\end{equation}
\begin{equation}\label{eq:duplicate}x=2\end{equation}
TODO：补充网格无关性分析。
\bibliography{references}
\end{document}
